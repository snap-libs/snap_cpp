{
    "bert_model_id": "kykim/bert-kor-base",
    "bert_model": "model_bert_int8.onnx",
    "interpret_as": "yomi",
    "description": "Korean TTS Frontend — BERT heads + phonology rules (V6: no MeCab/g2pk dependency)",
    "heteronym": {
        "conf_threshold": 0.6,
        "description": "동철이음이의어 경음화 판별 신뢰도 임계값. 이 값 미만이면 통계적 default(NONE)로 fallback."
    },
    "semiotic": {
        "enabled": true,
        "head_onnx": "model_semiotic.onnx",
        "label_map": "model_label_semiotic.json",
        "description": "기호 패턴 분류 (시간, 날짜, 점수, 비율 등 6클래스 neural + phone/ip 룰)"
    },
    "number": {
        "enabled": true,
        "description": "숫자 읽기 방식 분류 (룰 기반 단위 사전 — ONNX 불필요)"
    },
    "vowel_length": {
        "enabled": true,
        "long_dict": "phon_vowel_length.json",
        "description": "장단음 판별 (사전 기반, Val Acc 98.74%)"
    },
    "morph": {
        "enabled": true,
        "head_onnx": "model_morph.onnx",
        "label_map": "model_label_morph.json",
        "trie": "morph_trie.pkl",
        "description": "형태소 경계+POS (BIO-POS 76클래스, trie 기반, MeCab-free). BIO=93.5%, POS=94.7%. 조사의(JKG→에) 판별 포함."
    }
}
